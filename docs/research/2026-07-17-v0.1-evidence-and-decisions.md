# Research Run v0.1 Evidence and Decisions

## Purpose and claim ceiling

This document is the durable research and product brief behind the v0.1 kickoff.
It records what was inspected, what was learned, and which choices are binding
before implementation. It does not prove that the product exists or works.

Current claim ceiling: the product direction and acceptance contract are grounded
in reviewed source material and live project files. Implementation, installation,
runtime, packaging, GitHub pull-request, and real-user claims remain unproven until
the new development task produces fresh evidence on those surfaces.

## Current implementation authority update

Tree's direct instruction on 2026-07-17 supersedes the kickoff implementer's
initial Python stack choice before any implementation commit. Research Run v0.1
must use the layered core plus Rust overlay: a correctness-critical Rust CLI on a
pinned governed stable toolchain, Cargo as substrate, typed records and errors,
parse-before-effects, one claim authority, bounded deterministic storage,
atomic/recoverable workspace-confined writes, and fail-closed security.

The current activation is deliberately narrow. The repo inherits only the law
material to this product and acceptance through root `AGENTS.md`, `STANDARD.md`,
and `policy.toml`; Research Run is not a governance-distribution project. Rust is
the default product language. Python, TypeScript/SvelteKit, PostgreSQL, services,
observability infrastructure, and scale components have no measured v0.1 trigger
and are excluded. A future UI would require a new decision and would use Tree's
separate web overlay. The complete future governance zip is reconciliation input,
not a blocker to the current Rust walking slice.

The dependency decision is three small runtime crates: `clap` for typed CLI
parsing and help, `serde` for typed record boundaries, and `serde_json` for
canonical human-readable JSON. Standard-library filesystem primitives cover
bounded reads and atomic same-directory publication. Source, package, install,
runtime, journey, GitHub, release, and real-use evidence remain distinct.

## Reviewed evidence

### Supplied sessions and artifacts

- Session `019f50d9-7485-7080-be1a-c58de6747989`: the persisted session log was
  reviewed for GPT-5.6 routing and prompt law. The applicable result is one lean,
  outcome-first contract; mode, model, and reasoning are separate controls;
  requested routing is not effective-routing proof; stronger reasoning cannot
  compensate for missing context or acceptance checks.
- Session `019f669f-a52e-7782-ac24-867cb50ed35b`: the persisted session log, final
  steering, synthesis documents, founder and craft toolkits, mastery exam,
  GPT-5.6 addendum, source inventories, cutoff ledgers, design audit, repair notes,
  and runtime/accessibility reports were reviewed. Its reusable law is that an
  artifact, score, or deployment proves only its own surface. Product quality
  depends on a real journey, causal feedback, representative examples and
  anti-examples, recovery, transfer, and external use.

The session's exhaustive source inventories and cutoff ledgers were used to
reconcile the large raw research corpus without treating repeated discovery
records as independent support. Primary and high-impact links were opened where
they determine this contract.

### Live project sources

- `/Users/terrynoblin/Projects/2026-Nathan-Summer-Project`: current workspace law,
  product and reliability contracts, source cards, variant inventory, experiment
  receipt, crystallography workflow, and repository check were inspected. The
  strongest transferable mechanics are separated literature/design/lab/structure
  evidence; explicit claim ceilings; observations distinct from interpretation;
  negative results retained; raw data referenced rather than copied; and checks
  that never claim to prove the science.
- `/Users/terrynoblin/Projects/harness-ultragoal-plugin-proposal`: current workspace
  law, architecture, goal, product, reliability, and security contracts were
  inspected. The transferable mechanics are one semantic authority per effect;
  typed parsing before behavior; source/package/install/runtime/journey separation;
  atomic and recoverable effects; no hidden writes; and projections or receipts
  that cannot promote authority.
- `/Users/terrynoblin/Projects/research-run`: live Git, branch, remote, README, and
  the prior kickoff were inspected. The repository is intentionally minimal, so
  the new task must make the stack decision from first principles rather than
  copy a neighboring implementation.

### External foundations

- [OpenAI GPT-5.6 model guidance](https://developers.openai.com/api/docs/guides/latest-model),
  [prompt guidance](https://developers.openai.com/api/docs/guides/prompt-guidance-gpt-5p6),
  and [model selection](https://learn.chatgpt.com/docs/models): Sol should receive domain context,
  material constraints, approval boundaries, success criteria, and verification,
  without procedural micromanagement. Medium is the balanced reasoning route;
  Ultra is a separate subagent mode for genuinely independent work.
- [OpenAI Harness Engineering](https://openai.com/index/harness-engineering/):
  keep the repository legible to agents, store durable
  project knowledge locally, enforce important invariants mechanically, and allow
  local implementation freedom.
- [OpenAI Symphony](https://openai.com/index/open-source-codex-orchestration-symphony/),
  [ExecPlans](https://developers.openai.com/cookbook/articles/codex_exec_plans),
  and [iterative repair loops](https://developers.openai.com/cookbook/examples/codex/build_iterative_repair_loops_with_codex):
  one authoritative
  owner, isolated changes, structured state, living outcome plans, and
  review-repair-validation loops with failures fed back as typed input.
- [Parse, don't validate](https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/):
  preserve boundary knowledge in parsed types so invalid
  states do not remain representable throughout the program.

These sources establish design principles. They do not establish Research Run's
runtime behavior.

## Product decision

Three plausible first products were compared:

1. **Evidence-ledger CLI and workspace.** A researcher records sources, claims,
   evidence relationships, experiments, and current limits in a deterministic
   local format.
2. **Agent-ready project scaffolder.** A generator installs research-oriented
   contracts, folders, and checks into a new repository.
3. **Autonomous research orchestrator.** An agent plans searches and experiments,
   updates state, and coordinates research workflows.

Choose option 1. It provides direct researcher value without an AI provider,
turns the strongest Nathan-project practices into a reusable product, and creates
the typed state that future scaffolding or agents would need. Option 2 without a
working state model would ship ceremony. Option 3 would add trust, integration,
and evaluation burdens before the underlying evidence authority is proven.

The primary user is one graduate researcher or research engineer maintaining a
small, changing body of evidence. The first job is not "manage all research." It
is: **show me what we currently know, why we think it, what failed or remains
ambiguous, and what deserves attention next.**

## Canonical domain boundary

The implementation may refine names, but it must preserve these semantic roles:

- `ProjectManifest`: format version, project identity, and declared roots.
- `SourceRecord`: stable identity, citation or locator, provenance, and bounded
  notes. A source is not evidence for every claim by association.
- `ClaimRecord`: stable identity, exact claim text, scope, and human ownership.
- `EvidenceLink`: a typed relationship from a source, experiment, or artifact to
  one claim with a stance such as supports, limits, contradicts, or context, plus
  the specific evidence used.
- `ExperimentReceipt`: question, method or protocol reference, observations,
  interpretation, limitations, outcome, and next move. Negative and ambiguous
  outcomes are ordinary valid records.
- `ArtifactPointer`: workspace-safe or external locator, description, provenance,
  and optional digest. It is not an instruction to copy raw data.
- `ReviewDecision`: explicit human decision and rationale that may change the
  workspace assessment of a claim. AI authorship remains provenance, never
  authority.

Claim assessment is derived, not freely written. At minimum, deterministic status
must distinguish unsupported, limited, supported, contradicted, and unreviewed
states. Evidence links, generated prose, projections, retry, or file order cannot
silently promote a claim. "Supported" means supported within the declared
workspace evidence and review scope, not scientifically true in general.

Canonical records are the authority. Human and JSON status, indexes, context
packets, receipts, and summaries are projections that can be rebuilt. Publication
must be atomic and workspace-confined; an ambiguous prior effect must be inspected
before retry.

## Implementation graph

This is a dependency map for one Sol-medium implementer, not a request for agents
or parallel execution.

- `N0 Evidence freeze`: inspect the current repository, choose and record the
  stack, define the golden journey and adversarial fixtures.
- `N1 Authority foundation` depends on `N0`: versioned domain types, stable IDs,
  reference rules, status vocabulary, and parsing boundaries.
- `N2 Safe workspace` depends on `N1`: discovery, path confinement, atomic
  publication, recovery, and deterministic serialization.
- `N3 Research records` depends on `N1-N2`: sources, claims, evidence links, human
  reviews, and derived claim assessment.
- `N4 Experiment records` depends on `N1-N2`: experiment receipts, negative and
  ambiguous outcomes, protocol and artifact references.
- `N5 Product surface` depends on `N3-N4`: public CLI, human and JSON status,
  validation, diagnostics, and complete walking slice.
- `N6 Adoption surface` depends on `N5`: synthetic example, five-minute
  quickstart, packaging, and CI.
- `N7 Acceptance` depends on `N1-N6`: clean-clone journey, failure and recovery
  matrix, security/secret scans, bounded technical review, push, and pull
  request.

Build a thin end-to-end slice through `N1-N5` before filling every command. Keep
the public CLI, data contract, lockfile, fixtures, and compatibility policy under
single-task control. If future execution uses Ultra, only dependency-closed work
with disjoint semantic and path ownership should fan out.

## Skill selection

Every available Superpowers skill body was reviewed: `using-superpowers`,
`brainstorming`, `writing-plans`, `executing-plans`, `test-driven-development`,
`systematic-debugging`, `using-git-worktrees`, `requesting-code-review`,
`receiving-code-review`, `verification-before-completion`,
`finishing-a-development-branch`, `dispatching-parallel-agents`,
`subagent-driven-development`, and `writing-skills`.

Use their applicable practices: compare product alternatives; maintain a living
plan; work in an isolated managed worktree; prove a failing behavior before the
fix; diagnose causes rather than patch symptoms; evaluate review findings against
current code; and require fresh verification before claims. Adapt their procedural
defaults to the lean Sol prompt: do not stop for another speculative design gate,
do not turn the kickoff into code-filled microtasks, and do not use the parallel or
subagent workflows in this medium single-agent task.

Every available Product Design skill body was reviewed: `index`, `get-context`,
`user-context`, `research`, `ideate`, `audit`, `design-qa`, `image-to-code`,
`url-to-code`, and `share`.

Use `get-context` concepts to keep the user, job, constraints, risks, and
acceptance explicit. Defer the rest: there is no saved Product Design user context,
existing interface, visual target, or deployment to research, ideate visually,
audit, reproduce, QA, or share. After a real CLI journey exists, a product audit
of command ergonomics, information hierarchy, error recovery, and truthful status
may become useful; visual workflows remain out of scope unless evidence supports
a GUI.

## Acceptance and truth policy

The final task must map every requirement to current evidence. A source review
does not prove code. Code does not prove packaging. Unit tests do not prove the
public journey. A local binary does not prove GitHub or CI state. A pull request
does not prove release or adoption. A self-authored technical review may find
defects, but it does not prove external validation or real-world usefulness.

The product can claim v0.1 readiness only when the clean-clone journey and
representative valid, invalid, interruption, retry, path, symlink, malformed
input, unsupported-promotion, redaction, packaging, and secret-scan cases pass on
the integrated tree. Real-world usefulness and scientific impact remain withheld
until researchers use the tool on real projects and return evidence.
